@model IEnumerable<DAL.OrderedPhotography>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.NoOfPhotographers)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhotographyTitle)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.PhotographyDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhotographyCost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
       @using (Html.BeginForm("DeleteService", "ServicesToEvent"))
       {
        @Html.AntiForgeryToken()
        <td>
            @Html.DisplayFor(modelItem => item.NoOfPhotographers)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhotographyTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhotographyDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhotographyCost)
        </td>
        
        <td><input type="hidden" name="photoid" value="@item.PhotographyId" /></td>
        <td><input type="submit" value="Delete" class="btn btn-default" /></td>
        

        }

    </tr>
}

</table>
